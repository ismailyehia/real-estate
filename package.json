{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./scripts\" --if-present run typecheck",
    "start": "pnpm --filter @workspace/api-server start"
  },
  "private": true,
  "type": "module",
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "bcryptjs": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "mysql2": "catalog:",
    "tsx": "catalog:"
  }
}
